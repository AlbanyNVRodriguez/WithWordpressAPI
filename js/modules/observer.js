export default function observadorOpacity($item){
    let observador = new IntersectionObserver();
    let callback = function(entries){
        entries.forEach(entry => {
            
        });
    }
    observador.observe(callback, );
}