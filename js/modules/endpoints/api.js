
const DOMAIN = "",
    SITE = `${DOMAIN}/wp-json`,
    API_WP = `${SITE}/wp/v2/`,
    POSTS = `${API_WP}/posts`,
    PAGES = `${API_WP}/pages`,
    TAGS = `${API_WP}/tags`,
    CATEGORIES= `${API_WP}/categories`;

export default{
    DOMAIN,
    SITE,
    API_WP,
    POSTS,
    PAGES,
    TAGS,
    CATEGORIES
}